# Quickstart

## Requirements

* Python {{ python_versions[0] }} to {{ python_versions[-1] }}


## Installation

### Install from pypi:

This will install the latest release from pypi via pip:

```bash
pip install {{ project_name }}
```


## Using

### Running a dev server locally

You can run a local dev server easily:

```bash
make app/serve
```


### Running a dev server in docker compose

You can also run the server in docker compose:

```bash
docker compose up
```
